<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    <t t-name="chatter_google_message.chatter" t-inherit="mail.Chatter" t-inherit-mode="extension">
        <!-- Default Send message button is no longer overridden.
             Google message logic is handled transparently in chatter_patch.js. -->
    </t>
</templates>
