<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="upgrade_sync_config_view_form" model="ir.ui.view">
        <field name="name">res.config.settings.upgrade.sync</field>
        <field name="model">res.config.settings</field>
        <field name="priority">100</field>
        <field name="arch" type="xml">
            <form string="Upgrade Sync Configuration" class="oe_form_configuration">
                <header>
                    <button string="Save" type="object" name="execute" class="oe_highlight"/>
                    <button string="Discard" special="cancel"/>
                </header>
                <sheet>
                    <group string="Odoo 17 capture (source)">
                        <field name="upgrade_sync_enabled"/>
                        <field name="upgrade_sync_event_types" placeholder="All event types"/>
                    </group>
                    <div class="text-muted">
                        This database never contacts Odoo 19. The Odoo 19 test instance pulls the
                        captured events with a technical user in the group "Upgrade Sync Reader".
                    </div>
                </sheet>
            </form>
        </field>
    </record>

    <record id="upgrade_sync_config_action" model="ir.actions.act_window">
        <field name="name">Upgrade Sync Configuration</field>
        <field name="res_model">res.config.settings</field>
        <field name="view_mode">form</field>
        <field name="view_id" ref="upgrade_sync_config_view_form"/>
        <field name="target">inline</field>
    </record>
</odoo>
