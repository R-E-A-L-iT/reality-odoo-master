<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <template id="portal_my_home_company_wise" name="My Portal">
        <t t-call="portal.portal_layout">
            <t t-set="my_details" t-value="True"/>
<!--            <div class="o_portal_my_home">-->
                <div class="oe_structure" id="oe_structure_portal_my_home_1"/>
                <h3>my.R-E-A-L.iT</h3>
                <t t-if="companies" t-foreach="companies" t-as="comp">
                    <h4 class="mt-4 text-primary" t-esc="comp['company_name']"/>
                    <div class="o_portal_docs_companywise list-group">
                        <t t-if="comp['company_data']" t-foreach="comp['company_data']" t-as="comp_data">
                            <a t-att-href="comp_data['url']" t-att-title="comp_data['title']" class="list-group-item list-group-item-action d-flex align-items-center justify-content-between">
                                <t t-esc="comp_data['title']"/>
                                <span class="badge badge-secondary badge-pill" t-esc="comp_data['placeholder_count']" t-att-data-placeholder_count="comp_data['placeholder_count']"/>
                            </a>
                        </t>
                    </div>
                </t>
<!--            </div>-->
            <div class="oe_structure" id="oe_structure_portal_my_home_2"/>
        </t>
    </template>

    <template id="portal_my_rental_products" name="My Rental Products">
        <t t-call="portal.portal_layout">
            <t t-set="breadcrumbs_searchbar" t-value="True"/>
            <t t-call="portal.portal_searchbar">
                <t t-set="title">Rental Products</t>
            </t>
            <t t-if="not quotations">
                <p>There are currently no rental products for your account.</p>
            </t>
            <t t-if="quotations" t-call="portal.portal_table">
                <thead>
                    <tr class="active">
                        <th>Order #</th>
                        <th class="text-right">Order Date</th>
                        <th class="text-right">Product</th>
                        <th class="text-center"/>
<!--                        <th class="text-right">Subtotal</th>-->
                    </tr>
                </thead>
                <t t-foreach="quotations" t-as="quotation">
                    <t t-if="quotation.order_line" t-foreach="quotation.order_line" t-as="rental">
                        <tr>
                            <td><a t-attf-href="/my/orders/company/#{rental.order_id.id}/#{partner_company_id}?{{ keep_query() }}"><t t-esc="rental.order_id.name"/></a></td>
                            <td class="text-right"><span t-field="rental.order_id.date_order"/></td>
                            <td class="text-right"><span t-field="rental.product_id"/></td>
                            <td class="text-center">
                                <span t-if="quotation.state == 'cancel'" class="badge badge-pill badge-secondary"><i class="fa fa-fw fa-remove"/> Cancelled</span>
                                <span t-if="quotation.is_expired" class="badge badge-pill badge-secondary"><i class="fa fa-fw fa-clock-o"/> Expired</span>
                            </td>
<!--                            <td class="text-right">-->
<!--                                <span t-field="quotation.amount_total"/>-->
<!--                            </td>-->
                        </tr>
                    </t>
                </t>
            </t>
        </t>
    </template>

    <template id="portal_my_home_menu_rental_product" name="Portal layout : rental product menu entries" inherit_id="portal.portal_breadcrumbs" priority="20">
        <xpath expr="//ol[hasclass('o_portal_submenu')]" position="inside">
            <li t-if="page_name == 'rental_product'" class="breadcrumb-item active">
                Rental Products
            </li>
        </xpath>
    </template>

    <template id="portal_helpdesk_ticket_inherit_realit" inherit_id="helpdesk.portal_helpdesk_ticket">
        <t t-foreach="tickets" position="replace">
            <t t-foreach="tickets" t-as="ticket">
                <tr>
                    <td class="text-left"><a t-attf-href="/helpdesk/ticket/#{ticket.id}/#{partner_company_id}"><small>#</small><t t-esc="ticket.id"/></a></td>
                    <td><a t-attf-href="/helpdesk/ticket/#{ticket.id}/#{partner_company_id}"><span t-field="ticket.name"/></a></td><td/>
                    <td t-if="groupby != 'stage'" class="text-center"><span class="badge badge-pill badge-info" t-field="ticket.stage_id.name"/></td>
                </tr>
            </t>
        </t>
    </template>

<!--    <template id="portal_searchbar_realit" inherit_id="portal.portal_searchbar">-->
<!--        <xpath expr="//div[hasclass('input-group')]" position="attributes">-->
<!--            <attribute name="class">input-group input-group-sm w-100 mt-3</attribute>-->
<!--        </xpath>-->
<!--    </template>-->

    <template id="portal_my_orders_inherit" inherit_id="sale.portal_my_orders">
        <a t-att-href="order.get_portal_url()" position="replace">
            <a t-attf-href="/my/orders/company/#{order.id}/#{partner_company_id}?{{ keep_query() }}"><t t-esc="order.name"/></a>
        </a>
    </template>

    <template id="portal_my_quotations_inherit" inherit_id="sale.portal_my_quotations">
        <a t-att-href="quotation.get_portal_url()" position="replace">
            <a t-attf-href="/my/orders/company/#{quotation.id}/#{partner_company_id}?{{ keep_query() }}"><t t-esc="quotation.name"/></a>
        </a>
    </template>

    <template id="portal_my_invoices_inherit" inherit_id="account.portal_my_invoices">
        <t t-if="invoices" position="replace">
            <t t-if="invoices" t-call="portal.portal_table">
                <thead>
                    <tr class="active">
                        <th>Invoice #</th>
                        <th>Invoice Date</th>
                        <th class='d-none d-md-table-cell'>Due Date</th>
                        <th class="text-center">Status</th>
                        <th class="text-right">Total</th>
                    </tr>
                </thead>
                <tbody>
                    <t t-foreach="invoices" t-as="invoice">
                        <tr>
                            <td>
                                <a t-attf-href="/my/invoices/company/#{invoice.id}/#{partner_company_id}?{{ keep_query() }}" t-att-title="invoice.name">
                                    <t t-esc="invoice.name" t-if="invoice.name != '/'"/>
                                    <em t-else="">Draft Invoice</em>
                                </a>
                            </td>
                            <td><span t-field="invoice.invoice_date"/></td>
                            <td class='d-none d-md-table-cell'><span t-field="invoice.invoice_date_due"/></td>
                            <td class="tx_status text-center">
                                <t t-if="invoice.state == 'posted' and invoice.payment_state not in ('in_payment', 'paid', 'reversed')">
                                    <span class="badge badge-pill badge-info"><i class="fa fa-fw fa-clock-o" aria-label="Opened" title="Opened" role="img"></i><span class="d-none d-md-inline"> Waiting for Payment</span></span>
                                </t>
                                <t t-if="invoice.state == 'posted' and invoice.payment_state in ('paid', 'in_payment')">
                                    <span class="badge badge-pill badge-success"><i class="fa fa-fw fa-check" aria-label="Paid" title="Paid" role="img"></i><span class="d-none d-md-inline"> Paid</span></span>
                                </t>
                                <t t-if="invoice.state == 'posted' and invoice.payment_state == 'reversed'">
                                    <span class="badge badge-pill badge-success"><i class="fa fa-fw fa-check" aria-label="Reversed" title="Reversed" role="img"></i><span class="d-none d-md-inline"> Reversed</span></span>
                                </t>
                                <t t-if="invoice.state == 'cancel'">
                                    <span class="badge badge-pill badge-warning"><i class="fa fa-fw fa-remove" aria-label="Cancelled" title="Cancelled" role="img"></i><span class="d-none d-md-inline"> Cancelled</span></span>
                                </t>
                            </td>
                            <td class="text-right"><span t-esc="invoice.amount_total" t-options='{"widget": "monetary", "display_currency": invoice.currency_id}'/></td>
                        </tr>
                    </t>
                </tbody>
            </t>
        </t>
    </template>

    <template id="portal_my_home_menu_sale_inherit" inherit_id="sale.portal_my_home_menu_sale">
        <li id="portal_breadcrumbs_sale" position="replace">
            <li id="portal_breadcrumbs_sale" t-if="page_name == 'quote' or sale_order and sale_order.state in ('sent', 'cancel')" t-attf-class="breadcrumb-item #{'active ' if not sale_order else ''}">
                <t t-if="partner_company_id">
                    <a t-if="sale_order" t-attf-href="/my/quotes/company/#{partner_company_id}?{{ keep_query() }}">Quotations</a>
                    <t t-else="">Quotations</t>
                </t>
                <t t-else="">
                    <a t-if="sale_order" t-attf-href="/my/quotes?{{ keep_query() }}">Quotations</a>
                    <t t-else="">Quotations</t>
                </t>
            </li>
        </li>
        <xpath expr="//li[@t-elif]" position="replace">
            <li t-elif="page_name == 'order' or sale_order and sale_order.state not in ('sent', 'cancel')" t-attf-class="breadcrumb-item #{'active ' if not sale_order else ''}">
                <t t-if="partner_company_id">
                    <a t-if="sale_order" t-attf-href="/my/orders/company/#{partner_company_id}?{{ keep_query() }}">Sales Orders</a>
                    <t t-else="">Sales Orders</t>
                </t>
                <t t-else="">
                    <a t-if="sale_order" t-attf-href="/my/orders?{{ keep_query() }}">Sales Orders</a>
                    <t t-else="">Sales Orders</t>
                </t>
            </li>
        </xpath>
        
    </template>

    <template id="portal_my_home_menu_invoice_inherit" inherit_id="account.portal_my_home_menu_invoice">
        <li t-if="page_name == 'invoice'" position="replace">
            <li t-if="page_name == 'invoice'" t-attf-class="breadcrumb-item #{'active ' if not invoice else ''}">
                <t t-if="partner_company_id">
                    <a t-if="invoice" t-attf-href="/my/invoices/company/#{partner_company_id}?{{ keep_query() }}">Invoices &amp; Bills</a>
                    <t t-else="">Invoices &amp; Bills</t>
                </t>
                <t t-else="">
                    <a t-if="invoice" t-attf-href="/my/invoices?{{ keep_query() }}">Invoices &amp; Bills</a>
                    <t t-else="">Invoices &amp; Bills</t>
                </t>
            </li>
        </li>
    </template>

    <template id="portal_my_home_menu_helpdesk_inherit" inherit_id="helpdesk.portal_my_home_menu_helpdesk">
        <li t-if="page_name == 'ticket' or ticket or rating_page" position="replace">
            <li t-if="page_name == 'ticket' or ticket or rating_page" t-attf-class="breadcrumb-item #{'active ' if not ticket else ''}">
                <t t-if="partner_company_id">
                    <a t-if="ticket or rating_page" t-attf-href="/my/tickets/company/#{partner_company_id}?{{ keep_query() }}">Tickets</a>
                    <t t-else="">Tickets</t>
                </t>
                <t t-else="">
                    <a t-if="ticket or rating_page" t-attf-href="/my/tickets?{{ keep_query() }}">Tickets</a>
                    <t t-else="">Tickets</t>
                </t>
            </li>
        </li>
    </template>

</odoo>