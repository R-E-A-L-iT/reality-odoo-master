<?xml version="1.0" encoding="utf-8"?>

<odoo>
    <record id="action_renewal_map_form" model="ir.actions.act_window">
        <field name="name">Renewal</field>
        <field name="res_model">renewal.map</field>
        <field name="type">ir.actions.act_window</field>
        <field name="arch" type="xml">
            <form string="Renewal">
                <sheet>
                    <field name="id" />
                </sheet>
            </form>
        </field>
    </record>

    <menuitem
        id="proquotes_renewal_menu_item"
        name="Renewal Configuration"
        parent="sale.menu_sale_config"
        action="proquotes.action_renewal_map_form"
        sequence="1"
    />
</odoo>