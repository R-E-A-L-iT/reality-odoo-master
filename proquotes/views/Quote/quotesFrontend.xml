<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <template id="sale_order_portal_content" name="ProQuotes Sales Order Portal Content" inherit_id="sale.sale_order_portal_content" priority="17">
        
        <!-- 
        
        variables 

        -->
        <xpath expr="//div[@id='introduction']" position="before">
            
            <t t-set="language" t-value="request.lang.code" />
            <t t-set="sale_order" t-value="sale_order.with_context(lang=language)"/>

            <t t-if="sale_order.header_id.url[-4:] == '.jpg'" t-set="header_image" t-value="sale_order.header_id.url" />
            <t t-if="sale_order.header_id.url[-4:] == '.mp4'" t-set="header_video" t-value="sale_order.header_id.url" />

            <t t-set="company_name" t-value="sale_order.company_id.name"/>
        
        </xpath>

        <!-- 
        
        quote header 
        
        -->

        <!-- add image/video section -->

        <!-- 
        
        quote table head
        
        -->

        <!-- add classes for css -->

        <!-- add extra columns -->

        <!-- 
        
        quote line processing

        -->

        <!-- code for products -->
            <!-- add css class -->
            <!-- add description -->
            <!-- add checkbox/radio button -->
            <!-- add quantity selector -->

        <!-- code for bundles -->
            <!-- add css class -->
            <!-- add description -->
            <!-- add image -->
            <!-- add checkbox/radio button -->
            <!-- add quantity selector -->

        <!-- code for sections -->
            <!-- add css class -->
            <!-- parse section name -->
            <!-- add folding arrow -->
            <!-- add subtotal -->

        <!-- code for notes -->
            <!-- add css class -->
        

    </template>
    <template id="sale_order_portal_template" name="ProQuotes Sale Order Portal Template" inherit_id="sale.sale_order_portal_template" priority="17">
        <!-- remove sidebar -->
        <xpath expr="//div[@class='row o_portal_sale_sidebar']" position="replace" />
    </template>
</odoo>