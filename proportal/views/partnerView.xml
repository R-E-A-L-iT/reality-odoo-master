<?xml version="1.0" encoding="utf-8"?>

<odoo>
    <record id="view_partner_form" model="ir.ui.view">
        <field name="name">proportal.partner.form</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"></field>
        <field name="active" eval="True"/>
        <field name="arch" type="xml">
            <xpath expr="//notebook" position="inside">
                <page name="products" string="Products">
                    <sheet>
                        <!-- <div t-if="parent_id"> -->
                            <!-- <field name="parentProducts" string="Company Products"/> -->
                        <!-- </div> -->
                        <!-- <div t-if="company_type == 'company'"> -->
                            <field name="products" domain="[('company_id', '=', 1)]" string="Products"/>
                        <!-- </div> -->
                    </sheet>
                </page>
            </xpath>
            <xpath expr="//p[@invisible=&quot;type != 'contact'&quot;]" position="after">
                <p class="mb-0" invisible="type != 'renewal'">
                    <span>This contact will be treated as the default recipient for CCP renewal notifications.</span>
                </p>
            </xpath>
            <xpath expr="//form[.//field[@name='type' and @widget='radio']]//field[@name='name']/ancestor::group[1]" position="replace">
                <group>
                    <field name="name" string="Contact Name" required="type == 'contact' or type == 'renewal'"/>
                    <field name="title" options="{'no_open': True}" placeholder="e.g. Mr." invisible="type != 'contact' or type != 'renewal'"/>
                    <field name="function" placeholder="e.g. Sales Director" invisible="type != 'contact' or type != 'renewal'"/>
                    <label for="street" string="Address" invisible="type == 'contact' or type == 'renewal'"/>
                    <div invisible="type == 'contact' or type == 'renewal'">
                        <div class="o_address_format" name="div_address">
                            <field name="street" placeholder="Street..." class="o_address_street"/>
                            <field name="street2" placeholder="Street 2..." class="o_address_street"/>
                            <field name="city" placeholder="City" class="o_address_city"/>
                            <field name="state_id" class="o_address_state" placeholder="State" options="{'no_open': True, 'no_quick_create': True}" context="{'country_id': country_id, 'default_country_id': country_id, 'zip': zip}"/>
                            <field name="zip" placeholder="ZIP" class="o_address_zip"/>
                            <field name="country_id" placeholder="Country" class="o_address_country" options="{&quot;no_open&quot;: True, &quot;no_create&quot;: True}"/>
                        </div>
                    </div>
                </group>
                <group>
                    <field name="email" widget="email"/>
                    <field name="phone" widget="phone"/>
                    <field name="mobile" widget="phone"/>
                    <field name="company_id" invisible="1"/>
                </group>
            </xpath>
        </field>
    </record> 
</odoo>