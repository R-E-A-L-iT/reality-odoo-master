# -*- coding: utf-8 -*-

# Odoo 19 migration: temporarily disabled so the store uses default /shop category
# routing (this controller rewrote category URLs via the SEO-slug feature, whose
# templates/converter are already neutralized elsewhere).
# from . import website_sale
